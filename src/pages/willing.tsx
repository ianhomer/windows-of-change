import Layout from "../components/layout";
import Quote from "../components/quote";

export default function Page() {
  return (
    <Layout>
      <Quote who="Irvin Yalom, Pyschiatrist @ Stanford">
        I see willing as having two stages, a person initiates through wishing
        and then enacts through deciding.
      </Quote>
    </Layout>
  );
}
